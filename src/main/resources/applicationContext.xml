<?xml version="1.0" encoding="UTF-8"?>
<beans  xmlns="http://www.springframework.org/schema/beans"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns:context="http://www.springframework.org/schema/context"
        xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd">

    <!-- Creating music bean(specifying the class of needed music) and then we are creating a music player bean and inject musicBean via constructor-arg     -->
    <!-- Dependency Injection(DI) via constructor in the XML file   -->
    <bean id="musicBean"
          class="dlivitin.springapp1.RockMusic">
    </bean>

    <bean id="musicPlayer"
          class="dlivitin.springapp1.MusicPlayer">
        <constructor-arg ref="musicBean"/>
    </bean>

</beans>